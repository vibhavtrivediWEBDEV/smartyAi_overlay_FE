import Link from "next/link";
import { ArrowRight, Apple, Monitor } from "lucide-react";
import { PageHeading } from "@/components/dashboard-ui";
import { API_BASE_URL } from "@/lib/api";

export default function DashboardDownloadsPage() {
  return <><PageHeading eyebrow="Desktop app" title="Downloads" description="Install a current internal validation build. Public release readiness still depends on signing and clean-machine testing." /><div className="grid max-w-4xl gap-px border border-white/10 bg-white/10 md:grid-cols-2"><a href={`${API_BASE_URL}/downloads/${encodeURIComponent("SmartyAI-1.0.0-mac-arm64.dmg")}`} className="bg-[#12120f] p-7"><Apple className="text-gold" size={22} /><h2 className="display-type mt-7 text-3xl font-semibold">macOS Apple Silicon</h2><p className="mt-2 text-xs text-[#777066]">macOS 13+ · arm64 DMG</p><span className="mt-6 flex items-center gap-2 text-xs font-bold text-gold">Download <ArrowRight size={14} /></span></a><a href={`${API_BASE_URL}/downloads/${encodeURIComponent("SmartyAI-1.0.0-mac-x64.dmg")}`} className="bg-[#12120f] p-7"><Monitor className="text-gold" size={22} /><h2 className="display-type mt-7 text-3xl font-semibold">macOS Intel</h2><p className="mt-2 text-xs text-[#777066]">macOS 13+ · x64 DMG</p><span className="mt-6 flex items-center gap-2 text-xs font-bold text-gold">Download <ArrowRight size={14} /></span></a></div><div className="mt-5 max-w-4xl border border-white/10 p-5"><p className="text-sm font-bold">Windows status</p><p className="mt-2 text-xs leading-5 text-[#777066]">A final public Windows installer is not linked. Windows system-audio capture and offline transcription are included; microphone transcription and OCR remain unavailable.</p><Link href="/windows" className="mt-4 inline-flex items-center gap-2 text-xs font-bold text-gold">Read Windows support details <ArrowRight size={14} /></Link></div></>;
}